// If a number is even. multiply by eight. otherwise multiply the number by 9

function simpleMultiplication(number) {
    if(number % 2 === 0){
      return (number*8)
    }else{
      return (number*9)
    }
}
